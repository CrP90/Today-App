specVersion: "v1beta"
serviceId: "Today"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Today-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
